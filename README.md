## Guess The Country

A short description of the Guess The Country project will go here.

### Dependencies

- `express` — backend HTTP server
- `ejs` — server-side templates for the frontend

### How to Run

1. **Prerequisites:** Node.js and npm installed.
2. **Install dependencies:** From the project root, run `npm install`.
3. **Start the server:** Run `node backend/server.js`.
4. **Open in browser:** Go to `http://localhost:3000`.

### Project Structure

Details about the project structure will be added here.
